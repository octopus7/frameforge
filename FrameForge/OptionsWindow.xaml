<Window x:Class="FrameForge.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="옵션"
        Width="760"
        Height="500"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="#1B1B1B">
    <Grid Margin="12">
        <TabControl TabStripPlacement="Left"
                    Background="#1E1E1E"
                    BorderBrush="#333333"
                    Foreground="#F2F2F2">
            <TabItem Header="확장자 연결">
                <Border Margin="14"
                        Padding="18"
                        Background="#141414"
                        BorderBrush="#343434"
                        BorderThickness="1"
                        CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="#EFEFEF"
                                   Text="프로젝트 파일 확장자 연결" />

                        <TextBlock Grid.Row="1"
                                   Margin="0,10,0,0"
                                   Foreground="#BDBDBD"
                                   TextWrapping="Wrap"
                                   Text=".ffproj 파일을 FrameForge와 연결하면 탐색기에서 더블클릭으로 프로젝트를 바로 열 수 있습니다." />

                        <Border Grid.Row="2"
                                Margin="0,16,0,0"
                                Padding="10"
                                Background="#202020"
                                BorderBrush="#3F3F3F"
                                BorderThickness="1"
                                CornerRadius="6">
                            <TextBlock Foreground="#D6D6D6"
                                       TextWrapping="Wrap"
                                       Text="현재 사용자(HKCU) 범위로 등록됩니다. 관리자 권한은 필요하지 않습니다." />
                        </Border>

                        <StackPanel Grid.Row="3"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Margin="0,16,0,0">
                            <Button Width="130"
                                    Height="34"
                                    Margin="0,0,10,0"
                                    Click="AssociateExtensionButton_Click"
                                    Content="확장자 연결" />
                            <Button Width="90"
                                    Height="34"
                                    IsCancel="True"
                                    Content="닫기" />
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
